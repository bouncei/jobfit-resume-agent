# ======================================
# REQUIRED CONFIGURATION
# ======================================

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ======================================
# OPTIONAL CONFIGURATION
# ======================================

# OpenAI Model Selection (optional - defaults to gpt-4o-mini)
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
# Note: GPT-4 models provide better quality but cost more
OPENAI_MODEL=gpt-4o-mini

# Google Docs API Configuration (optional - enables Google Docs integration)
# Leave empty to disable Google Docs integration (app will work without it)
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project and enable Google Docs API
# 3. Create OAuth 2.0 Desktop credentials and download as credentials.json
GOOGLE_CREDENTIALS_PATH=credentials.json

# Google Token Storage (optional - defaults to token.json)
# This file will be created automatically during first authentication
GOOGLE_TOKEN_PATH=token.json

# User Information (optional - used for cover letter personalization)
# This can also be provided via command line: --user-name "Your Name"
USER_NAME=Your Full Name

